@{
    ViewData["title"] = "Nuevo Presupuesto";
    var ListaClientes = ViewBag.Clientes as List<Clientes>;
}

@model Presupuestos

<section>
    <form asp-controller="Presupuestos" asp-action="CargarPresupuesto" method="post">
        <section class="form-group">
            <label asp-for="@Model.Cliente.ClienteId">Seleccione un cliente: </label>
            <select asp-for="@Model.Cliente.ClienteId" class="form-control">
                <option value="">-- Seleccione --</option>
                @foreach (var cliente in ListaClientes)
                {
                    <option value="@cliente.ClienteId">@cliente.Nombre</option>
                }
            </select>
        </section>

        <section class="form-group">
            <label asp-for="@Model.FechaCreacion">Fecha: </label>
            <input type="date" asp-for="@Model.FechaCreacion" class="form-control" value="@DateTime.Now">
        </section>

        <button type="submit" class="btn btn-primary my-3">Siguiente</button>
    </form>
</section>